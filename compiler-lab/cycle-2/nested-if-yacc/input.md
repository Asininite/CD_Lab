# Sample input for `nested-if-yacc`

Tests nested if statements.

Sample stdin:

if (a > b) {
    if (b > c) {
        max = a;
    }
}

To run: compile and run the parser, then paste the snippet above.
