# Sample input for `while-yacc`

A small while loop snippet to test parsing.

Sample stdin:

while (i < 10) {
    i = i + 1;
}

To run: compile the parser/lexer and paste the snippet above into stdin.
